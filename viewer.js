document.addEventListener("DOMContentLoaded", () => {

    console.log("Reading Territory — A Proposition in Formation");

    Mirador.viewer({
        id: "mirador-viewer",
        windows: [
            {
                manifestId: "manifest.json"
            }
        ]
    });

});